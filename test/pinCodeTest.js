process.env.NODE_ENV = 'test';

var PinCode = require("../api/models/pinCodeModel");
var PinCodeController = require("../api/controllers/pinCodeController");

describe("PINCODE", (done) => {
    
    before((done) => {
        
    });
    
    after((done) => {
        
    });
    
    it("Criar pinCode", (done) => {
        
    });

});